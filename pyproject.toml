[project]
name = "abc-ebill"
version = "0.1.0"
description = "Convert 农行信用卡电子对账单 .eml into xlsx tables and reconcile"
requires-python = ">=3.9"
dependencies = ["openpyxl>=3.1.0", "wcwidth>=0.2.13"]

[tool.uv]
# default behavior: create .venv in project root on first `uv run`
